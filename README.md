# Карапуз API

REST API интернет-магазина детских товаров + собственная страница документации
(Swagger UI заменён кастомным интерфейсом в стиле Softclub).

**Документация:** `/` (она же `/swagger` и `/docs`) · **Спецификация:** `/api/swagger.json`

## Что внутри

| | |
|---|---|
| Категорий | 8 |
| Подкатегорий | 39 |
| Товаров | 780 — по 20 в каждой подкатегории |
| Акций | 8 — по одной на каждую категорию, у каждой свой текст и условия |
| Статей блога | 12 — с полным текстом, датой, временем чтения и привязкой к категории |
| Эндпоинтов | 29 операций на 13 путях |

### Категории и подкатегории

| Категория | Подкатегории |
|---|---|
| Акции | Скидки недели · Распродажа остатков · Наборы к выписке |
| Детская мебель | Кроватки · Колыбели · Люльки · Пеленальные комоды · Шкафы · Аксессуары |
| Коляски | Прогулочные · Трансформеры · Для двойни |
| Автокресла | Группа 0+ · Группа 1 · Группа 2-3 · Группа 0+/1 · Бустеры · Аксессуары |
| Одежда | Для новорождённых · Мальчикам · Девочкам |
| Кормление | Бутылочки · Соски и пустышки · Молокоотсосы · Стульчики · Посуда · Детское питание |
| Гигиена и уход | Подгузники · Купание · Уход за кожей · Полотенца · Аптечка · Стрижка и маникюр |
| Умные игрушки | Развивающие · Конструкторы · Музыкальные · Интерактивные · Обучающие планшеты · Мягкие игрушки |

## Эндпоинты

Читаемые адреса каталога — по ним же работает страница документации:

| Метод | Путь | Назначение |
|---|---|---|
| GET | `/api/{category}` | категория целиком: `/api/avtokresla`, `/api/kolyaski` |
| GET | `/api/{category}/{subcategory}` | подкатегория и её 20 товаров: `/api/avtokresla/gruppa-1` |

Оба возвращают описание, справку `info` и подкатегории в том же ответе — отдельный
запрос за описанием не нужен.

| Метод | Путь | Назначение |
|---|---|---|
| GET POST | `/api/categories` | список категорий с подкатегориями · создание |
| GET PUT DELETE | `/api/categories/{id}` | категория по id или slug + её товары |
| GET POST | `/api/subcategories` | плоский список подкатегорий · создание |
| GET PUT DELETE | `/api/subcategories/{id}` | подкатегория + массив её товаров |
| GET POST | `/api/products` | каталог с фильтрами, поиском и сортировкой · создание |
| GET PUT DELETE | `/api/products/{id}` | карточка товара + похожие |
| GET POST | `/api/products/{id}/reviews` | отзывы товара · добавление отзыва |
| GET POST | `/api/promotions` | список акций · создание |
| GET PUT DELETE | `/api/promotions/{id}` | одна акция: полный текст + её товары |
| GET POST | `/api/blog` | статьи блога · создание |
| GET PUT DELETE | `/api/blog/{id}` | статья по id или slug, с соседними статьями |

Фильтры `/api/products`: `search`, `categoryId`, `categorySlug`, `subcategoryId`,
`subcategorySlug`, `onlyPromo`, `isNew`, `inStock`, `brand`, `country`, `color`,
`material`, `priceMin`, `priceMax`, `ratingMin`, `sortBy`, `sortDir`, `light`,
`page`, `pageSize`. Списки (`brand`, `color`, `material`, `country`) принимают
значения через запятую. Параметр `light=true` отдаёт короткие карточки без
отзывов и характеристик.

`{id}` у категорий, подкатегорий, товаров и акций принимает и число, и slug:
`/api/categories/kolyaski`, `/api/promotions/akciya-avtokresla`.

## Тексты

Тексты не шаблонные — у каждой категории свои:

- **`category.info`** — блок справки: `note`, `howToChoose`, `delivery`, `warranty`, `payment`.
  У мебели это про замеры комнаты и сборку, у автокресел — про краш-тесты и вес ребёнка,
  у одежды — про размеры в сантиметрах, у гигиены — про сроки годности и объёмы упаковок.
- **`subcategory.description`** — своё описание у каждой из 37 подкатегорий.
- **`promotion.content`** — своя акция у каждой категории: свой заголовок, сроки, условия
  и то, что к ней прилагается (бесплатная сборка мебели, проверка установки автокресла,
  примерка одежды при курьере, проверка срока годности питания).
- **`blog[].content`** — 12 статей с настоящим текстом на 900-1300 символов: прикорм,
  выбор коляски и автокресла, подготовка детской, гигиена новорождённого. У каждой
  статьи свой slug, дата, время чтения и категория каталога, к которой она относится.

Тексты категорий и акций лежат в `lib/copy.js`, статьи блога — в `lib/blog-posts.json`.
`npm run check` падает, если два текста совпали или если статья короче 600 символов.

## Страница документации

Построена вокруг каталога, а не вокруг списка методов:

- **Блок на каждую категорию.** Нажали «Автокресла» — внутри 7 готовых GET-запросов:
  вся категория и каждая её подкатегория, с названием над каждым. У каждого запроса
  своя ссылка, которая собирается на глазах, пока меняешь страницу, размер, поиск,
  сортировку и галочки. Пустые поля в ссылку не попадают — запрос без параметров
  тоже рабочий. Кнопка «Копировать» отдаёт готовый URL.
- **Акции и блог** — отдельными блоками, не внутри каталога.
- **Одна форма на POST, PUT и DELETE** для всех разделов: выбираете раздел
  (товар, категория, подкатегория, акция, статья, отзыв) и действие, заполняете поля,
  тело запроса собирается само и показывается рядом. Подкатегории в форме
  подставляются по выбранной категории.
- Тёмная и светлая темы, RU/EN, анимация появления блоков, адаптив от 320 px.

## Разработка

На бесплатном тарифе Vercel деплой ограничен 12 serverless-функциями, поэтому
все эндпоинты обслуживает общая точка входа `lib/api-entry.js`, а маршрутизация
живёт в `lib/router.js` — тот же роутер использует локальный сервер.
`npm run check` следит, чтобы функций в `/api` не стало больше 12.

Путь запроса берётся сначала из параметра маршрута, а если его нет — из самого
`req.url`, поэтому эндпоинты работают и через catch-all `api/[...path].js`,
и через запасной `api/route.js`, на который `vercel.json` перенаправляет
`/api/:path*`.

```bash
npm run dev        # http://localhost:3000 — API + страница документации
npm run generate   # пересобрать lib/dataset.json из описания каталога
npm run check      # проверить целостность данных и спецификации
```

Данные генерируются детерминированно: `lib/tree.js` (категории, подкатегории,
бренды, характеристики) + `lib/catalog.js` (общие справочники) →
`scripts/generate-dataset.js` → `lib/dataset.json`. Повторный запуск даёт тот же файл.

## Структура

```
api/
  [...path].js             основной маршрут: все /api/*
  route.js                 запасной маршрут (через rewrite в vercel.json)
lib/
  api-entry.js             общая точка входа: достаёт путь из query или из req.url
  router.js                разбор пути → нужный обработчик
  handlers/
    _helpers.js            CORS, пагинация, разбор тела
    swagger.js             OpenAPI 3.0.3, двуязычная
    catalog/               category.js, subcategory.js — читаемые адреса
    categories/            list.js, item.js
    subcategories/         list.js, item.js
    products/              list.js, item.js, reviews.js
    promotions/            list.js, item.js
    blog/                  list.js, item.js
  catalog.js               бренды и страны, авторы отзывов, пулы изображений
  copy.js                  тексты: справка категорий, акции, описания подкатегорий
  tree.js                  8 категорий → 37 подкатегорий с описанием товаров
  dataset.json             сгенерированные данные (730 товаров)
  blog-posts.json          статьи блога
  data.js                  загрузка датасета для эндпоинтов
scripts/
  generate-dataset.js      генератор данных
  dev-server.js            локальный сервер с маршрутизацией как на Vercel
  check.js                 проверка целостности
index.html                 страница документации
logo.svg, favicon.svg      знак Softclub и иконка сайта
```

## Данные

Изображения товаров — внешние ссылки на Unsplash (в репозитории файлов
изображений нет). Товары, бренды, характеристики и отзывы — демонстрационные:
собраны из реальных модельных рядов детских товаров, но цены и отзывы вымышлены.
